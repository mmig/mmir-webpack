/* placeholder file: will be replaced with app-specific configuration during webpack compilation */
module.exports = function({appConfigCode}){
    // NOTE: this return value will replace the module during webpack build
    return { code: appConfigCode };
};
